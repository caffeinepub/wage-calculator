{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Show payroll summary below wage entries",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Make the PayrollSummary component always visible below the WageEntryTable on the WageCalculator page",
      "acceptanceCriteria": [
        "The PayrollSummary card is rendered on the WageCalculator page below the WageEntryTable without requiring a button click or toggle.",
        "All four key values are displayed: Steuer-Brutto, SV-Brutto, Nettoentgelt, and Auszahlung.",
        "The summary updates automatically when wage entries are added, edited, or deleted.",
        "Loading skeletons are shown while the payroll summary is being fetched.",
        "The layout is responsive and visually consistent with the existing design."
      ],
      "file_operations": [
        {
          "path": "frontend/src/pages/WageCalculator.tsx",
          "operation": "modify",
          "description": "Remove the conditional rendering logic for PayrollSummary and render it directly below WageEntryTable in the main container. Ensure the component remains reactive to wage entry changes by keeping the existing React Query integration intact."
        }
      ]
    }
  ]
}